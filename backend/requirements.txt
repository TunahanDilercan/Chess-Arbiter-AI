# ── Phase 1: Chess core ─────────────────────────────────────────────────��──────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-chess>=1.10.0

# ── Phase 2: Database + Upload ────────────────────────────────────────────────
sqlalchemy>=2.0.0
asyncpg>=0.29.0              # async PostgreSQL driver
alembic>=1.13.0              # schema migrations
python-multipart>=0.0.6     # UploadFile / Form support in FastAPI

# ── Phase 3: Computer Vision ──────────────────────────────────────────────────
opencv-python-headless>=4.8.0
scipy>=1.11.0                  # signal.find_peaks for grid line detection
Pillow>=10.1.0                 # synthetic test image generation

# ── Phase 3: Celery / Redis (worker infrastructure) ───────────────────────────
celery>=5.3.0
redis>=5.0.0

# ── Phase 4: OCR ──────────────────────────────────────────────────────────────
anthropic>=0.40.0                # Claude Vision OCR (primary backend)
transformers>=4.36.0             # TrOCR model + processor (offline fallback)
torch>=2.1.0                     # inference engine (CPU or CUDA)
# pytesseract>=0.3.10            # dev/comparison only — uncomment if needed
# boto3>=1.34.0                  # S3 storage (activate with S3_BACKEND=s3)

# ── Phase 5+: Candidate Matching (uncomment when needed) ──────────────────────
# python-levenshtein>=0.23.0

# ── Development / Testing ──────────────────────────────────────────────────────
httpx>=0.25.0
pytest>=7.4.0
pytest-asyncio>=0.21.0
aiosqlite>=0.19.0            # SQLite async driver (tests only — not used in production)
anyio>=4.0.0
