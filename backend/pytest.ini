[pytest]
asyncio_mode = auto
# Disable the anyio pytest plugin: no test uses anyio, and its auto-loaded
# plugin conflicts with pytest-asyncio's AUTO mode, intermittently breaking
# async-test collection later in the session ("async def not natively supported").
addopts = -p no:anyio
